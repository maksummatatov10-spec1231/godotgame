# 🗺️ Как сделать свою карту, спавн, лут и камеру (Godot 4.3, рус.)

Всё делается мышкой в редакторе, без кода. Игровая сцена — `scenes/main.tscn`.

## ⚡ Шаг 0. Открыть проект правильно
1. Скачай zip (ссылка в релизе) и **полностью распакуй** его в НОВУЮ папку.
   (Старую папку проекта и папку `.godot` удали — Godot кэширует старые скрипты!)
2. В Godot: **Импорт** → найди в распакованной папке файл **`project.godot`** → Открыть.
3. **F5** — игра. Всё, никаких дополнительных файлов не нужно.

🎓 **Сначала запусти обучение:** открой сцену **`scenes/demo_map.tscn`** (двойной клик
в панели FileSystem) и нажми **F6** («Запустить текущую сцену»). Это живая учебная карта:
- подписи прямо на карте показывают, где маркер старта героя, точки спавна врагов и лута;
- строка нарисованных тайлов-предметов на старте превращается в объекты;
- колонны и стены показывают, как сложить непроходимые препятствия;
- в дереве сцены (слева) видно, как устроены узлы `SpawnPoints`, `PickupPoints`,
  `PlayerStart`, второй слой `Decor` и своя `Camera2D`.
Повтори эти узлы в своей `main.tscn` — и получишь то же самое на своей карте.

---

## 0. Где CharacterBody / Collision / Camera? (короткий ответ)

**Их нет — и это специально.** Физика Godot здесь не используется:

| Что | Как сделано вместо физики |
|---|---|
| **Стены (коллизии)** | Кодом по тайлам слоя **Arena**: строки тайлсета `0, 4, 5, 7` = стены, остальное = пол. Игрок/враги/снаряды проверяют клетки напрямую |
| **Удары и подбор предметов** | По расстоянию между центрами (`distance_to`) — быстрее и проще, чем Area2D |
| **Камера** | Создаётся кодом в `main.gd` и цепляется к герою. Границы = твоя карта |

Поэтому **ничего добавлять не нужно** — просто рисуй карту (шаг 1).

> Если очень хочешь видеть камеру в дереве сцены — добавь `Camera2D` куда угодно,
> игра сама подхватит её и привяжет к герою (позиция сбросится на героя, зум — в настройках Main).

---

## 1. Слой Arena — стены и пол (единственный слой с коллизиями)

1. Выдели узел **Main → Arena** в дереве сцены.
2. Внизу откроется панель **TileMap** → вкладка **Плитки (Tiles)**.
3. Выбирай тайлы и рисуй прямо по сцене:
   - ЛКМ — поставить тайл, ПКМ — стереть
   - `Shift`+ЛКМ — линия, с прямоугольником — заливка областями

### Какие тайлы — стены
Строка в атласе (второе число координаты) решает всё:
- **Строки `0, 4, 5, 7`** → СТЕНА (не пройти, снаряды гаснут)
- **Все остальные строки** → пол (проходимо)
- **Пустая клетка** (ничего не нарисовано) → пустота, тоже непроходима

> Координаты тайла видны внизу панели TileMap при наведении (**атласные координаты X,Y**).
> Хочешь другие правила стен — поправь массив `WALL_ROWS` в `arena.gd`.

### Порядок рисования стен (как в базовой арене)
- **Верхняя стена**: строка `y=2` → тайлы `(1,0)…(5,0)` по кругу; строка `y=3` → `(0,4)…(5,4)`
- **Нижняя стена**: ряд 4, затем ряд 5 (`(0,5)…(2,5)`)
- **Боковые стены**: один столбец ряда 4
- **Пол**: любые тайлы из `(1..4, 1..3)`

### Автокраска
Узел **Arena** → Инспектор → **`default_paint`**:
- галка стоит + карта пустая → код нарисует базовую арену 64×36 (можно потом править мышкой);
- галка снята → игра НИЧЕГО не дорисует, карта полностью твоя.

---

## 2. Второй слой — декор с «ожившими» предметами ✨

Твой слой (у тебя он называется `TileMapLayer`, ребёнок Arena или Main — неважно)
используй для красоты: **он ничего не блокирует**, игрок пройдёт сквозь нарисованные тайлы.
Обязательно выставь ему то же `tile_set` (`res://assets/tiles/dungeon.tres`).

**Главное:** если нарисуешь на нём предметы из двух нижних рядов атласа —
на старте игры тайл стирается и на его месте появляется **настоящий объект**:

| Тайл (x,y) | Что появится |
|---|---|
| `(6,8)` монета | монета (+1 опыт) |
| `(7,8)`, `(7,9)` синие флаконы | флакон опыта (+6) |
| `(9,8)` красный флакон | лечение +20 |
| `(8,9)` большой красный | лечение +35 |
| `(8,8)`, `(9,9)` ключи | ключи (+10 / +20 опыта) |
| `(0..3,8)` большие сундуки | открываемый сундук с лутом |
| `(4,8)`, `(5,8)` мини-сундуки | мини-сундук (3 монеты) |
| `(0,9)`, `(1,9)` факелы | **горящий** анимированный факел |
| `(2,9)`, `(3,9)`, `(5,9)` свечи/кубок | горящие свечи |

Остальные тайлы (знамёна, черепа, пьедесталы `(4,9)`, `(6,9)` и т.п.) остаются просто картинкой-декором.

---

## 3. Где спавнятся враги — настройка

**Без ничего:** враги появляются кольцом вокруг героя (радиус — в настройках Main, см. шаг 5).

**Свои точки спавна:**
1. Правой кнопкой по **Main** → Добавить узел → **Node2D** → назови `SpawnPoints`.
2. В него добавь сколько хочешь **Marker2D** и расставь их мышкой по карте
   (двери, коридоры — ставь на проходимый пол!).
3. Готово: обычные враги, элитки и боссы полезут из этих точек (со случайным разбросом ±10px).

Также работают **группы**: выдели любые маркеры → панель Узел → Группы → добавь в группу
`enemy_spawn` — эффект тот же. Удобно, если точки разбросаны по разным узлам.

---

## 4. Точки лута и старт героя

- Узел **`PickupPoints`** (Node2D) с маркерами внутри (или группа `pickup_spawn`) —
  там будут появляться периодические сундуки/флаконы/монеты.
  Нет такого узла — лут сыплется кольцом вокруг героя.
- Маркер **`PlayerStart`** (ребёнок Main, или группа `player_start`) — точка появления героя.
  Иначе — центр арены (512, 300 для базовой карты).

Из врагов тоже падает лут: монеты (30–70% по типу), красные флаконы, синие,
зелёные эликсиры и редкие серебряные ключи; с босса — сундук, большой флакон и **золотой ключ**.*

---

## 5. Настройки Main в Инспекторе (без кода)

Выдели **Main** → Инспектор → группы настроек:

| Параметр | По умолчанию | Что делает |
|---|---|---|
| `spawn_radius` | 170 | радиус кольца спавна вокруг героя (если нет SpawnPoints) |
| `interval_start` | 1.15 | сек между спавнами в начале |
| `interval_min` | 0.30 | самая частая скорость спавна (к концу игры) |
| `cap_start` / `cap_max` | 14 / 64 | лимит врагов на карте: старт / максимум |
| `world_loot` | вкл | сыпать ли монеты/флаконы на карту каждые ~20 сек |
| `camera_zoom` | 1.0 | зум камеры: 1.5 — ближе, 0.8 — дальше |
| `camera_smooth` | 6.0 | плавность догона камеры |

Состав волн по времени и расписание боссов — в `scripts/data.gd` (`WAVE_TABLE`, `BOSS_SCHEDULE`).

---

## 6. Стреляй-ловушка, шипы и прочее

Шипы и разбросанные ящики/флаг ставятся кодом **только для базовой арены**
(функция `_decorate()` в `main.gd`). На своей карте рисуй декор своим слоем (шаг 2).

---

## 7. 🚪 Живые двери

Нарисуй на слое **Arena** тайл **(6,2)** (деревянная дверь) или **(8,3)** (щитовая):
- **Закрытая** — непроходима для ГЕРОЯ, ВРАГОВ и СНАРЯДОВ (стена).
- Подойди — дверь **трещит** (тайл (7,2), скрип) и **вскрывается** → остаются обломки (проходимо).
- Враги тоже выламывают двери, но в ~4.5 раза дольше.
- На базовой и учебной картах двери уже стоят (внизу по центру / в верхнем проёме).

## 8. 📋 Каждый тайл атласа: коллизия и поведение

Атлас 10×10 (`assets/tiles/dungeon_tileset.png`). Координаты — (x,y).

| Ряд y | Что там | Коллизия / поведение |
|---|---|---|
| **0** | Крышки стен (0..5) | 🧱 **СТЕНА** |
| **1** | Пол (0..4), лестница-темнота (5..9) | ✅ пол |
| **2** | Пол (0..3), стена-боковина (4,5), **ДВЕРЬ (6,2)** 🚪, дверь треснувшая (7,2), рама-ящик (8,2), лестницы (9) | ✅ пол, **дверь = живая** |
| **3** | Пол (0..4), обломки двери (6,3)/рамы (7,3), **щит-дверь (8,3)** 🚪, лестница (9,3) | ✅ пол, **дверь = живая** |
| **4** | Лицо стены | 🧱 **СТЕНА** |
| **5** | Низ стены — тень (0..2), полка (5), лестницы на стене (6..8), горшок+ящик (9) | 🧱 **СТЕНА** (подошва/лестницы приставные) |
| **6** | Пол треснувший (0..3), паутина (4,5), обломки двери (6,6)/рамы (7,6), кость (8,6), гладкий пол (9,6) | ✅ пол |
| **7** | Пол тёмный (0..3), знамя/цепи/крюк/череп (4..7) | 🧱 **СТЕНА** (висит на стенах) |
| **8** | Сундуки (0..5), монета (6), флакон синий (7), ключ (8), флакон красный (9) | ✨ **оживает** в объект |
| **9** | Факелы (0,1), подсвечник (2), свечи (3,5), пьедесталы (4,6), флаконы большие (7,8), ключ золотой (9) | ✨ факелы/свечи/флаконы/ключ **оживают**; пьедесталы — декор |

**Оживление** = на старте тайл заменяется объектом (предметом/огнём), на слое Arena
под ним остаётся пол. Работает на ЛЮБОМ TileMap-слое сцены.

## 9. 🔊 Звуки и музыка

Все звуки — `assets/sfx/` (wav/mp3), собраны из 5 бесплатных паков, громкость каждого
отбалансирована вручную (таблица `LEVELS` в `scripts/sfx.gd`: минус = тише, плюс = громче).
Хочешь свои — **просто замени файлы с теми же именами**, код трогать не надо:
`shoot slash hit enemy_die elite_die player_hurt player_die coin gem flask resurrect key chest
crate levelup boss boss_die gameover win click spike door doorbreak spawn step vampire_shot comet_hit`.

Фишки звукового движка (`scripts/sfx.gd`):
- **Варианты**: к имени можно добавить `_2` … `_5` (`coin.wav`, `coin_2.wav`… до `coin_5.wav`) —
  при каждом проигрывании выбирается случайный вариант, звук не приедается.
  У отдельного варианта может быть СВОЯ громкость: запись в `LEVELS` с полным именем
  (`"coin_5": -1.0`) перекрывает базовую (`"coin": -8.0`).
- **Музыка зациклена**: `music_menu.mp3` (меню), `music_main.mp3` (бой),
  `music_boss.mp3` (орган на весь бой с боссом!), `music_tuto.mp3` (обучающая сцена),
  `music_win.mp3` (сады после победы). У каждого трека своя громкость — `MUSIC_LEVELS`
  поверх базовой `MUSIC_DB`. Для wav луп включается через `loop_mode = LOOP_FORWARD`,
  для mp3/ogg — свойством `loop = true` (код уже делает это сам).
- У каждого звука ±6% случайной высоты тона для живости.

---

### Памятка по файлам
- `assets/tiles/dungeon_tileset.png` — картинка атласа 10×10 тайлов 16px
- `assets/tiles/dungeon.tres` — сам TileSet
- `scripts/arena.gd` — правила стен (`WALL_ROWS`), базовая арена
- `scripts/main.gd` — точки спавна, оживление декор-слоя, камера, настройки
