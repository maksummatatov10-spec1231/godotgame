# 🗺 Как нарисовать свою карту (Godot 4.3, русский интерфейс)

## Где что лежит
- **Тайлсет**: `game/assets/tiles/dungeon.tres` (атлас: `game/assets/tiles/dungeon_tileset.png`, клетка 16×16)
- **Сцена карты**: `game/scenes/main.tscn` → узел **`Arena`** (тип `TileMapLayer`)
- **Логика стен**: `game/scripts/arena.gd`

## Шаг 1. Открой карту в редакторе
1. Открой `scenes/main.tscn`
2. В дереве сцены выбери узел **`Arena`**
3. Внизу экрана появится панель **«TileMap»** — это редактор тайлов
4. Справа в панели виден весь тайлсет 10×10 — кликай по тайлам и рисуй на сцене
   (ЛКМ — рисовать, ПКМ/«Ластик» — стирать, есть режимы линия/прямоугольник/заливка)

## Шаг 2. Что считается стеной (коллизии!)
В игре коллизии — **кодовые** (физика не используется, см. ниже).
При движении игра спрашивает: клетка под точкой **стена**, если:
- клетка **пустая**, **или**
- её **строка атласа** входит в `WALL_ROWS` в `game/scripts/arena.gd`:

| Строка атласа (y) | Что там | Тип |
|---|---|---|
| **0** | верх стены (кирпич) | 🧱 СТЕНА |
| **1–3** | чистый пол + дверь/лестницы | ✅ пол |
| **4** | лицо стены (богатая кирпичная) | 🧱 СТЕНА |
| **5** | низ стены с тенью | 🧱 СТЕНА |
| **6** | обломки/мусор | ✅ пол (проходимо) |
| **7** | знамя, черепа (декор стен) | 🧱 СТЕНА |
| **8** | сундуки, куча монет | ✅ пол (декор) |
| **9** | факелы/свечи статичные | ✅ пол (декор) |

> Хочешь другие правила — просто поправь массив `WALL_ROWS` в `arena.gd`.
> Координаты тайла видны внизу панели TileMap при наведении (**атласные координаты X,Y**).

## Шаг 3. Порядок рисования стен (как в базовой арене)
Для красивой сплошной стены клади тайлы **полосами**:
- **Верхняя стена**: строка `y=2` → тайлы ряда 0 (`(1,0)…(5,0)` по кругу), строка `y=3` → ряд 4 (`(0,4)…(5,4)`)
- **Нижняя стена**: ряд 4, затем ряд 5 (`(0,5)…(2,5)`)
- **Боковые стены**: один столбец ряда 4
- Пол: случайные тайлы из `(1..4, 1..3)` — любой из 12 смотрится хорошо

Или просто запусти игру один раз с пустой картой — код сам нарисует
базовую арену 64×36, а ты отредактируешь её в редакторе (правки сохранятся в сцене).

## Шаг 4. Отключить автокраску
Узел `Arena` → в Инспекторе свойство **`default_paint`** → **сними галочку**,
если рисуешь карту полностью сам (иначе при отсутствии любых клеток подставится базовая арена).

## Что подстроится автоматически
- **Границы игры** = твои нарисованные клетки минус 2 тайла стен по краям
- **Камера** ограничена картой
- **Игрок/враги** скользят вдоль стен (осевое скольжение), враги не пройдут сквозь стены
- **Снаряды** (дротики, сферы, кометы) гаснут, попадая в стены

## Декор и ловушки
Факелы, шипы, ящики, флаг ставятся кодом — функция `_decorate()` в `game/scripts/main.gd`.
Поменял карту — обнови их координаты там (или удали лишние).

## Про масштаб клеток
Размер не ограничен: рисуй любые комнаты/коридоры. Только оставь игроку
стартовое место ~по центру (512, 300) или поправь позицию в `main.gd` (`player.global_position`).
